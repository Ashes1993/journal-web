
services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Aa0015858553@
      POSTGRES_DB: journal_db
    volumes:
      - ./postgres-data:/var/lib/postgresql/data